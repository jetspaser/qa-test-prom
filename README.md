# 🛠 UI Automation Framework (miner)

Этот проект представляет собой автоматизированный фреймворк для тестирования сайта **miner.ru**. 
Построен на базе **Python** и **Playwright** с использованием архитектурного паттерна **Page Object Model (POM)**.

---

## 🏗 Стек технологий

* **Python 3.10+** — основной язык программирования.
* **Playwright** — современный инструмент для автоматизации браузеров.
* **Pytest** — мощный фреймворк для написания и запуска тестов.
* **GitLab CI** — настройка непрерывной интеграции для автозапуска тестов.

---

## 📂 Структура проекта

* `pages/` — Page Object классы (описание элементов и логики страниц).
* `tests/` — Тестовые сценарии, разделенные по функциональным блокам.
* `conftest.py` — Глобальные фикстуры (настройка селекторов и контекста браузера).
* `pytest.ini` — Файл конфигурации параметров запуска Pytest.
* `.gitlab-ci.yml` — Инструкции для GitLab CI/CD пайплайна.
* `.gitignore` — Список исключений для Git (виртуальное окружение, логи, кэш).



---

## 🚀 Быстрый старт

### 1. Подготовка окружения
Клонируйте репозиторий и перейдите в папку проекта:
```bash
cd qa-test-prom

---

Установка зависимостей:

# Установка pytest-playwright через py
py -m pip install pytest-playwright

# После установки, установка браузеров Playwright
py -m playwright install

pip install pytest-playwright

playwright install



Команды:

Зайти в папку: cd qa-test-prom

Вернуться на уровень выше: cd ..



Запуск Pytest для конкретного файла:

Чтобы не гонять все тесты проекта, используй путь к файлу после команды pytest:



Полезные флаги (Шпаргалка):

--headed: чтобы увидеть браузер (по умолчанию Playwright в pytest запускает его в фоне/headless).

--slowmo 1000: замедлить действия робота на 1 секунду (чтобы ты успел понять, куда он кликает).

-s: выводить все print() в консоль (если ты добавил их для отладки).

-x: остановиться сразу после первой упавшей ошибки (удобно при дебаге).



Пример «боевой» команды для дебага:

pytest pytest tests/test_main_page.py --headed --slowmo 500 -v
